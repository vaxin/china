# 第二十四垂直切片：城市人物连续生命线结构设计

## 决策

人物仍采用“Worker 权威状态 + Babylon 帧间插值”的双层模型。模拟层决定身份、道路格、阶段与岗位；渲染层只把相邻格状态平滑播放，不反向修改世界。

## 模块边界

- `protocol`：新增可选 `citizens` 快照字段和严格引用校验。旧 v6 存档缺省为空。
- `simulation/citizen-life.ts`：纯函数负责岗位需求、确定性分配和生命周期转移。
- `simulation/index.ts`：每 tick 在住户结算后协调市民；拆除/断路时进行安全稳定化；快照、克隆和 hydration 保留状态。
- `renderer`：用稳定复合键区分流民、市民和乐师；统一插值器，根据角色/状态选择行走或工作帧，并让人物永远绘制在道路与建筑之前的可读层级。
- `App`：HUD 增加“市民在途/工作/休息”可观察计数，便于验证城市是否真的有人。

## 状态机

`commuting → working → returning → resting → commuting`

- 路径每 tick 只前进一个正交道路格。
- `working` 与 `resting` 各保留一个 tick，确保动作可见。
- 岗位消失或不可达：优先返家；返家也不可达则原地 `waiting`，补路后恢复。
- 每户仅一个代表；住宅拆除则删除，岗位拆除则重新分配。

## 视觉方向

沿用已经锁定的高保真古代中国手绘写实风格，但人物比旧流民略提亮、轮廓更明确。职业通过服装与手持物识别，不使用头顶图标：居民为布衣包袱，农夫持锄，工匠系围裙持工具，官差持简牍。动画的记忆点是“道路上持续的小规模人流”，不增加无关 UI 装饰。
