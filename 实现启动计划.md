# 3D 网页城市沙盘：实现启动计划

> 日期：2026-07-10  
> 假设：1–4 人小团队、桌面浏览器优先、风格化 3D、单机优先。若团队规模更大，任务可并行，但验收顺序不变。  
> 第一目标：6 周内交付一个可公开试玩前的内部垂直切片，证明技术可行和核心循环成立。

## 1. 现在先锁定的项目定义

一句话版本：

> 玩家在可旋转的 3D 中国古代沙盘中，通过道路、供水、粮食和服务网络，使聚落成长为有生命的城市；所有运行问题都可观察、可解释、可解决。

首个版本只证明下面这条链：

```mermaid
flowchart LR
  A[铺道路] --> B[放住宅]
  B --> C[居民迁入]
  C --> D[产生劳动力]
  D --> E[井、农场、磨坊、市场运行]
  E --> F[水与粮食送达]
  F --> G[住宅升级、人口增加]
  G --> H[更多需求与布局压力]
  H --> E
```

以下内容不进入首个 6 周切片：完整朝代、军事、外交、宗教英雄、大型纪念工程、随机地图生成、多人、账号、云存档、正式剧情和完整美术资产。

## 2. 开工前 2–3 天要产出的东西

### 2.1 决策与规范

- 确认项目临时代号；不要使用“龙之崛起”“皇帝”等可能造成混淆的商业名称。
- 把 [网页版技术选型.md](./网页版技术选型.md) 作为 ADR-001，变更核心技术必须补充决策记录。
- 把 [龙之崛起重制_研究与设计基线.md](./龙之崛起重制_研究与设计基线.md) 作为玩法研究输入，不将原作数据或资产放进仓库。
- 建立来源台账：模型、纹理、字体、音频、历史资料都记录来源、作者、许可证与可商用状态。
- 统一代码规则：TypeScript strict、格式化、lint、测试命名、提交规范、PR 检查。

### 2.2 临时视觉规则

先用灰盒，仍需统一尺寸，否则模型和代码会反复返工：

| 项目 | 首版约定 |
| --- | --- |
| 地面格 | 1 tile = 4×4 米（游戏世界单位） |
| 地形高度 | 每级 1 米；首版只允许少量离散高度 |
| 道路 | 普通道路 1 tile；主路/河渠后续支持多 tile |
| 相机 | 正交投影；默认方位角约 45°；限制俯仰；90° 旋转 |
| 模型原点 | 建筑地基 footprint 中心；Y=0 为地基面 |
| 坐标 | X/Z 为平面，Y 为高度；规则层只认 tileX/tileY/elevation |
| 模型格式 | glTF/GLB，米制，应用 transform，统一朝向 |
| 颜色 | 灰盒按住宅/生产/服务/物流类别着色 |

临时性能预算必须写进资产校验器：普通建筑控制材质数量，重复植物/道路/围墙必须可实例化；角色先用胶囊或极简模型，不在第一阶段制作骨骼动画。

### 2.3 验证环境

- 一台主开发机加至少一台较低配独显/核显设备。
- Chrome、Edge、Firefox、Safari 当前稳定版。
- 记录设备、分辨率、浏览器、渲染后端和测试日期，避免只说“我这里很流畅”。

## 3. 6 周里程碑

### 第 0 周：仓库与护栏（2–3 天）

交付物：项目能安装、启动、测试和构建；空白 3D 场景可部署到测试地址。

- 初始化 pnpm workspace、Vite、React、Babylon.js、Vitest、Playwright。
- 建立 `game-web / simulation / protocol / renderer / content-schema / persistence` packages。
- 配置 `tsc --noEmit`、lint、格式化、单测、构建和浏览器冒烟。
- 建立错误边界、开发日志、FPS/tick/draw-call 性能 HUD。
- CI 每次变更运行类型检查、单测、构建和一条浏览器冒烟。

验收：全新环境按照 README 能在 10 分钟内启动；CI 全绿；浏览器显示空场景、版本号和性能 HUD。

### 第 1–2 周：3D 技术切片

交付物：一个没有正式玩法、但能证明技术路线的压力场景。

功能：

- 160×160 可见地图，另提供 512×512 压测地图。
- 正交相机平移、缩放、90° 旋转、边界约束和指针落格。
- chunk 地形、水面占位、格子高亮、建筑预览与合法/非法反馈。
- 实例化树木、道路和建筑占位；点击可识别 tile 与 entity。
- 一个 Simulation Worker 以固定 tick 运行计数世界。
- typed command：放置、拆除、暂停、倍速。
- 增量 snapshot 驱动 Babylon 表现，不从 React 写游戏状态。
- IndexedDB 保存/读取地图和 tick；刷新后恢复。
- WebGL2 默认，WebGPU 开关仅用于记录对比。

验收：

- 10,000 个静态实例和 1,000 个移动占位单位下，1080p 主力测试机 60 FPS；低配档至少稳定 30 FPS。
- 正常速度的 Worker 不阻塞相机和 UI；10× 快进不会造成消息队列无限增长。
- 同种子、同命令序列在 Node 测试与浏览器 Worker 中得到相同 checksum。
- 保存、刷新、加载后地图与关键状态一致。
- 四个目标浏览器可以进入、建造、保存和加载；差异有记录。

停止条件：若不满足，先解决分块、实例化、材质、阴影、Worker 消息和存档问题，不进入玩法堆叠。

### 第 3–4 周：最小核心循环

交付物：玩家能建成一个可运转的小聚落。

内容只做：道路、住宅、井、农场、磨坊、市场、仓库七类建筑；水、粮食、劳动力三类关键资源。

- 移民通过地图入口抵达有道路连接的空住宅。
- 住宅提供人口与劳动力，有“缺道路/缺水/缺粮/无空位”原因卡。
- 井产生供水服务；服务采用距离场/服务区，不使用随机步行者决定规则。
- 农场按周期产粮，仓库保存，市场发起配送任务。
- 物流采用库存预留；货物不得负数或被多个任务重复领取。
- 住宅满足水和粮后升一级，失去供应后有宽限期再降级。
- UI 展示人口、就业、库存、时间倍率、选中建筑详情和水/粮覆盖层。

验收：新玩家无需控制台和攻略，15 分钟内能建成 50 人稳定聚落；任一住宅未升级时，两次点击内能看到主要原因；拆断关键道路后系统能显示受影响对象并在恢复道路后自愈。

### 第 5–6 周：垂直切片与第一次试玩

交付物：20–30 分钟完整内部试玩。

- 一张手工河谷地图。
- 一个明确目标：在期限内达到指定人口与住宅等级。
- 一个中期变化：旱季降低井/农田效率，迫使玩家调整储备或布局。
- 收入/建造成本形成简单预算约束。
- 教程提示、成功/失败、重新开始、自动存档。
- 基础环境音和少量反馈音；灰盒升级为一组统一风格的代表性模型。
- 完成 5–8 名目标玩家试玩，记录完成率、卡点、操作日志和访谈。

验收：至少 70% 试玩者不靠口头指导完成第一个稳定供水与粮食闭环；失败者能说明主要原因；没有阻断流程的存档、寻路或浏览器兼容问题。

## 4. 首个迭代的工程任务清单

按依赖顺序实施：

1. Workspace、CI、开发/生产构建。
2. Babylon engine、Scene 生命周期和渲染后端选择。
3. 正交相机、输入、世界坐标与 tile picking。
4. 地图数据结构与 chunk mesh。
5. 建筑占地、预览、放置/拆除命令。
6. Worker 启动、固定 tick、暂停和倍速。
7. command/snapshot/event 协议、序号、拒绝原因。
8. renderer view model 与 instance 管理。
9. 存档 schema、Dexie、保存/加载与 checksum。
10. 性能 HUD、压力场景和自动基准测试。
11. Playwright：启动 → 放置 → 保存 → 刷新 → 加载。
12. README、调试说明和已知限制。

任何任务未形成可测试的用户结果，都不算完成。例如“完成地图系统”太宽；应拆成“160×160 地图可显示、落格、放置、序列化，并通过边界测试”。

## 5. 首批协议与数据结构

先冻结最小协议，避免 React、渲染器和模拟互相引用内部对象：

```ts
type GameCommand =
  | { seq: number; type: 'build'; buildingTypeId: string; x: number; y: number; rotation: 0 | 1 | 2 | 3 }
  | { seq: number; type: 'demolish'; entityId: number }
  | { seq: number; type: 'set-speed'; speed: 0 | 1 | 2 | 5 | 10 };

type CommandResult =
  | { seq: number; accepted: true; appliedAtTick: number }
  | { seq: number; accepted: false; reasonCode: string; details?: Record<string, unknown> };

interface BuildingDefinition {
  id: string;
  footprint: { width: number; height: number };
  cost: Record<string, number>;
  modelId: string;
  tags: string[];
}
```

具体实现可调整，但要坚持：稳定 ID、协议可版本化、拒绝原因机器可读、内容定义不引用 Babylon 对象。

## 6. 美术准备与技术美术验证

首月只制作一套“风格样板”，不要批量生产：

- 一块地形、一个水面、一路一墙。
- 一间平民住宅的 2 个升级阶段。
- 一座井、一个农场、一座市场。
- 一棵树、两种作物、一个居民占位角色。
- 白天、黄昏两种光照截图；低配/高配两档对比。

样板需验证：远中近三档可读性、四个旋转方向、建筑入口位置、地基贴合、选中轮廓、覆盖层着色、材质复用、实例化和加载体积。通过后才制定完整 Art Bible 和批量外包规范。

建议的风格不是写实：适当夸张屋顶轮廓、入口、功能性道具和颜色分区；相机较远时建筑功能仍要一眼可辨。真实尺度可以适度压缩，但同类资产必须统一。

## 7. 测试先行的关键规则

从第一条玩法规则开始就写可重复测试：

- 建筑 footprint 不越界、不重叠，旋转后尺寸正确。
- 住宅只有与入口连通时允许移民。
- 同一份库存不能被两条物流任务同时消费。
- 任意 tick 后库存、人口、劳动力不为负。
- 暂停期间世界状态不改变；快进与逐 tick 执行结果一致。
- 删除道路会失效相关路径缓存，但不会留下悬空任务。
- 存档版本迁移失败时保留原存档并给出可恢复错误。
- 表现层丢帧、隐藏行人或降低动画频率不改变模拟结果。

## 8. 第一次试玩要收集的数据

- 到达第一批移民、稳定供水、稳定粮食、首次升级各用时。
- 建造后立即拆除/撤销次数。
- 打开覆盖层和原因卡的次数。
- 首次失败前的道路断连、缺工、缺货和库存积压事件。
- 试玩完成率、游玩时长、是否愿意再开一局。
- 访谈只问事实与决策：“你刚才为什么建这里？”“你当时以为哪里出了问题？”

不要用“喜欢吗”代替可行动证据。

## 9. 当前最大风险与预案

| 风险 | 早期信号 | 预案 |
| --- | --- | --- |
| 3D 美术成本失控 | 一个建筑超过数天且材质不复用 | 风格化模块、共享材质、先样板后批量 |
| 大量对象拖慢浏览器 | draw call 与 scene node 随城市线性爆炸 | chunk、instance/thin instance、LOD、表现代理 |
| Worker 通信过量 | 每帧发送完整世界、GC 抖动 | 增量快照、可见区订阅、transferable buffer |
| 寻路尖峰 | 拆路后卡顿、请求队列增长 | 连通域、缓存失效、每 tick 预算、分层寻路 |
| 模拟难以解释 | 玩家知道缺货但不知道为什么 | 原因码、物流任务追踪、覆盖层、事件日志 |
| 过早扩范围 | 第一条闭环未通就做朝代/战争 | 以 6 周验收线作为范围闸门 |
| 原作知识产权风险 | 仓库出现提取资产/逐字数据 | 来源台账、原创命名与美术、clean-room 规则 |

## 10. 达到什么状态才算“准备完成”

满足以下条件即可开始正式实现：

- 3D 技术选型和三层边界已接受。
- 首个 6 周切片范围和“不做清单”已接受。
- tile 尺度、相机、模型格式和临时资产规则已确定。
- 仓库、CI、测试浏览器和测试设备可用。
- 首个迭代任务已拆到 0.5–2 天粒度，每项有验收标准。
- 原创临时代号和来源台账已建立。
- 至少预留 5 名目标试玩者。

完成这些后，第一条代码路径应是：**空白 Babylon 场景 → 正交相机 → tile picking → 放置一个灰盒建筑 → 通过 Worker 确认 → 刷新后从存档恢复**。这条路径一次贯通了渲染、输入、协议、模拟和持久化，是整个项目最有价值的第一块地基。

