# 第二垂直切片：道路连通与民居入住验收

**执行记录**：[第二垂直切片-验收.execution-log.md](./第二垂直切片-验收.execution-log.md)

## 目标与范围

本轮贯通“铺路 → 道路网络连到城门 → 民居获得道路服务 → 下一模拟 tick 入住 → 自动保存与刷新恢复”。道路和人口都由 Worker 内的权威世界决定，3D 只负责表现。

本轮不包含道路拆除、曲线/桥梁、行人寻路、就业、税收、幸福度、住宅升级或逐户家庭成员。

## 已采用假设

- [假设 H2-01] 地图入口固定为 tile `(0,15)`；只有从该 tile 通过上下左右相邻道路可达的道路属于“已连通”。
- [假设 H2-02] 道路占 1×1 格，不可越界、与住宅重叠或重复铺设；协议支持一次原子提交一条连续路径，当前 UI 先以单格点击铺设。
- [假设 H2-03] 住宅只要占地边缘上下左右相邻至少一个已连通道路格，就具备道路服务。
- [假设 H2-04] 每座具备道路服务的住宅在下一次模拟 tick 入住 5 人；无服务住宅保持 0 人。
- [假设 H2-05] 浏览器每秒请求推进 1 tick；纯模拟层可一次推进多个 tick，结果必须确定且可复现。
- [假设 H2-06] 道路/人口改变世界 revision 并进入按 revision 串行的自动保存；纯空转 tick 不触发存档写入。
- [假设 H2-07] 新存档格式升级为 v2；第一切片 v1 存档自动迁移为空道路、空住户的 v2 当前世界，不隔离、不丢住宅。

## 领域与横切维度

| 维度 | 结论 | 场景 |
| --- | --- | --- |
| 权威数据 | 道路、连通性、住户和 tick 都在 simulation/Worker；渲染不参与判定 | S2-01～S2-08 |
| 原子性 | 路径整条接受或整条拒绝，不允许半条道路 | S2-02、S2-03 |
| 时间确定性 | 相同世界与 tick 数得到相同人口结果 | S2-04、S2-05 |
| 兼容性 | v1 迁移；v2 往返；坏档继续隔离 | S2-06、S2-07 |
| 并发/顺序 | build、tick 和 autosave 按 revision 保序，旧快照不能覆盖新快照 | S2-08 |
| UX/本地化 | 工具选中、绿/红预览、道路数、人口数与拒绝原因均可见且为简体中文 | S2-01、S2-03、S2-05 |

## ATDD 验收场景主表

| ID | 场景 | 分类 | Given | When | Then | 自动化层级 |
| --- | --- | --- | --- | --- | --- | --- |
| S2-01 | 单格道路可铺设并显示 | 成功/UX | 空世界已就绪 | 选择“道路”并点击合法空格 | 出现 1×1 路面；道路数为 1；提示“道路铺设完成”；自动保存完成 | E2E + 集成 |
| S2-02 | 连续道路路径原子落地 | 成功/数据 | 一条路径内所有格合法且正交连续 | Worker 收到 `build-road-path` | 整条路径一次加入；revision 只增加 1；道路数增加路径长度 | 单元 + 集成 |
| S2-03 | 非法道路整条拒绝 | 失败/边界 | 路径越界、断裂、重复，或任一格与住宅/道路重叠 | 提交路径 | 返回明确拒绝原因；道路、住宅、人口与 revision 均不变；UI 红色预览 | 单元 + E2E |
| S2-04 | 未连城门的道路不会带来人口 | 失败/业务 | 住宅邻接一段与入口不连通的道路 | 推进任意正数 tick | 人口仍为 0；无住户生成 | 单元 |
| S2-05 | 连通道路使住宅在下一 tick 入住 | 成功/时间 | 住宅邻接从 `(0,15)` 连通的道路；当前人口 0 | 推进 1 tick | 该住宅住户为 5；总人口为 5；HUD 在 2 秒内更新 | 单元 + Worker 集成 + E2E |
| S2-06 | 第一切片 v1 存档无损迁移 | 兼容 | v1 存档含合法住宅 | 载入当前版本 | 住宅坐标/ID/revision 保留；roads/households 为空；不进入 quarantine | 协议 + 持久化集成 |
| S2-07 | v2 道路和人口刷新后恢复 | 成功/持久化 | 已有连通道路、住宅和 5 人，保存状态为“已保存” | 刷新页面 | 道路、住宅、人口和最后一次物质变化对应的 tick 恢复且不重复；显示“已恢复” | 持久化集成 + E2E |
| S2-08 | tick 与建造并发不会让旧存档覆盖新世界 | 并发/数据 | build 与 tick 响应临近发生 | 连续调度自动保存 | 最终 autosave 的 revision 为已调度最高 revision；刷新不丢道路或人口 | 单元 + 集成 |

## 开发闸门与确认

用户于 2026-07-10 明确授权：“你后面可以自己验收和驱动一直往下走了”。本轮在该授权下自检：成功路径、失败/边界、时间状态、存档迁移、并发顺序、UI 与跨层测试均已覆盖，范围没有扩大到新产品方向。结论：**验收基线确认，可以进入结构设计和 TDD 产码**。

## 执行结果

| 项 | 结果 |
| --- | --- |
| TDD 单元 / 集成 | 8 个测试文件、50 条用例全部通过；覆盖 v1/v2、原子道路、入口洪泛、tick、人口、保存队列和竞态 |
| Chromium ATDD | 全仓 12 条全部通过，其中第二切片 S2-01、S2-03、S2-05/S2-07 共 3 条 |
| Firefox / WebKit | 第一启动场景各 1 条 WebGL2 冒烟通过；WebKit 自动化不等同于原生 Safari 手工验收 |
| 静态质量 | TypeScript strict（含 E2E/工具）、ESLint 零警告、Prettier 全部通过 |
| 生产构建 | Vite `dist` 构建和真实 Worker 产物通过；全部 E2E 从生产预览执行 |
| 数据兼容 | 合法 v1 原位迁移为 v2；非法 v1/v2 事务内隔离；迟到旧 revision 不覆盖新档 |

最终门禁：`pnpm test:all`，2026-07-10 全绿。非阻塞观察：Babylon 主入口约 1.29 MB minified / 330 KB gzip，继续保留为后续按需加载任务。
