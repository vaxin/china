# 第一垂直切片：3D 建造与自动恢复验收

**执行记录**：[第一垂直切片-验收.execution-log.md](./第一垂直切片-验收.execution-log.md)

## 迭代前置与输入

| 项 | 内容 |
| --- | --- |
| 目标 / 范围 | 贯通浏览器 3D 渲染、格子交互、Worker 权威建造和 IndexedDB 自动恢复 |
| 主要读者 | 产品、开发、测试 |
| 相关文档 | [实现启动计划.md](./实现启动计划.md)、[网页版技术选型.md](./网页版技术选型.md) |
| 环境假设 | 桌面 Chrome / Edge / Firefox / Safari 当前稳定版；WebGL2；键鼠输入；简体中文 |

## 本轮范围

本轮只交付一条可运行的技术垂直链路：

> 打开 3D 沙盘 → 操作相机 → 选择灰盒住宅 → 在合法格子建造 → Worker 确认 → 页面显示建筑 → 自动保存 → 刷新后恢复。

本轮不包含道路、居民、资源生产、住宅升级、撤销、正式美术、音频、账号、服务器或多人。

## 已采用的假设

- [假设 H-01] 首张技术地图固定为 32×32 平地；地图逻辑坐标为 `x=0..31, y=0..31`。
- [假设 H-02] 首个建筑只有“灰盒住宅”，占地 2×2 格，可按 90° 旋转；旋转不改变其方形占地。
- [假设 H-03] 建造模式通过页面上的“住宅”按钮进入；合法预览为绿色，非法预览为红色。
- [假设 H-04] 建造成功后立即异步自动保存；页面刷新以最后一次完整成功的存档为准。
- [假设 H-05] 建筑越界或与已有建筑重叠都属于拒绝建造，拒绝原因用简体中文显示，且不会扣费或生成建筑。
- [假设 H-06] 本切片没有经济系统，建造不消耗资源；状态栏只显示建筑数量、模拟 tick 和保存状态。
- [假设 H-07] 相机支持鼠标中键/右键拖动画面、滚轮缩放、Q/E 旋转 90°；具体按键后续可配置。
- [假设 H-08] 自动化 E2E 以 Chromium 为合并闸门；Firefox、WebKit 运行冒烟并记录引擎差异。

## 待确认问题

无阻塞问题。本轮所有可自行判断的细节均已作为 H-01～H-08 明示；用户若确认本验收文档，即视为接受这些假设用于第一切片。

## 领域与横切维度

| 项 | 结论 | 对应场景 |
| --- | --- | --- |
| 领域系统形态 | N/A；本轮无支付、订单、仓储、权限、计费、运营配置等业务形态。Worker 消息是内部实现边界，不按外部实时业务展开。 | 全部 |
| 体验 UX | 覆盖加载、就绪、合法/非法反馈、保存状态和错误可恢复。 | S-01～S-07 |
| 逆向 / 撤销 | 本轮明确不实现撤销；拒绝建造必须保持世界状态不变。 | S-04、S-05 |
| 多入口 | N/A；建造仅从工具栏进入。 | S-02 |
| 本地化 | 本轮关键状态与错误使用简体中文，不出现 `undefined` 或内部错误堆栈。 | S-01、S-04～S-07 |
| 观测 | 开发 HUD 显示 FPS、tick、建筑数与渲染后端；生产错误页不暴露堆栈。 | S-01、S-07 |

## ATDD 验收场景主表

| 场景 ID | 场景名称 | 分类 | Given（前置） | When（动作） | Then（可观察结果） | 自动化层级 |
| --- | --- | --- | --- | --- | --- | --- |
| S-01 | 首次打开即可看到可操作的 3D 沙盘 | 成功 / UX | 当前浏览器支持 WebGL2；站点没有已有存档 | 用户打开游戏页面并等待加载完成 | 页面显示 32×32 绿色灰盒地面、建筑工具栏和“游戏已就绪”；HUD 显示 WebGL2、建筑数 0；控制台无未处理异常 | E2E + 手工视觉 |
| S-02 | 合法空地可以建造一座住宅 | 成功 | 游戏已就绪；地图上没有建筑；用户进入住宅建造模式 | 用户把指针移到完整容纳 2×2 占地的空地并单击 | 单击前显示绿色 2×2 预览；单击后出现一座灰盒住宅；建筑数变为 1；界面显示“住宅建造完成”；建造命令结果为 accepted | E2E + 集成 |
| S-03 | 相机移动后仍能准确落格建造 | 成功 / 交互 | 游戏已就绪且未建造；用户处于住宅建造模式 | 用户拖动、缩放并旋转相机后，在新的可见空格单击 | 相机画面按操作改变；预览仍贴合指针所指地格；最终住宅出现在该地格而非旋转前的屏幕位置 | E2E + 手工视觉 |
| S-04 | 已占用土地明确拒绝重复建造 | 失败 | 地图坐标 `(10,10)` 已有一座占地 2×2 的住宅；建筑数为 1 | 用户再次尝试在与其重叠的范围建造 | 预览为红色；单击后不新增建筑，建筑数仍为 1；界面显示“该位置已被占用”；命令结果为 rejected/occupied | E2E + 单元 + 集成 |
| S-05 | 地图边界外不能产生建筑 | 边界 / 参数非法 | 32×32 地图已就绪；住宅占地 2×2 | 系统收到会使任一占地格超出 `0..31` 的建造命令，例如 `x=31,y=31` 或负坐标 | 命令被拒绝为 out-of-bounds；建筑数和地图占用均不改变；通过 UI 触发时显示“建筑不能超出地图边界” | 单元 + 集成；E2E 覆盖右下边缘 |
| S-06 | 成功建造会在刷新后恢复 | 成功 / 数据状态 | 用户已成功建造一座住宅；自动保存状态已经显示“已保存” | 用户刷新页面并等待游戏重新就绪 | 住宅在相同 tile 坐标与朝向恢复；建筑数仍为 1；不重复生成；HUD 显示存档版本与“已恢复” | E2E + 持久化集成 |
| S-07 | 损坏或不兼容的存档不会造成白屏 | 异常 / 可恢复 | IndexedDB 中存在无法通过 schema 校验的存档 | 用户打开或刷新游戏页面 | 页面仍进入空白 32×32 新地图；显示“旧存档无法读取，已为你创建新游戏”；损坏记录被隔离而非覆盖；控制台无未处理异常 | 集成 + E2E |
| S-08 | 快速重复点击不会重复落下多座建筑 | 边界 / 交互 | 用户处于住宅建造模式；目标 2×2 空地合法；第一条建造命令尚在 Worker 处理中 | 用户对同一位置快速双击 | 最终最多出现一座住宅；建筑数为 1；后续重复命令被拒绝或合并，用户不会看到叠模 | E2E + 集成 |

## 与测试数据的对应

| 场景 ID | fixture / 数据 | E2E 步骤摘要 | 关键断言 |
| --- | --- | --- | --- |
| S-01 | 清空 IndexedDB | 打开 `/` → 等待 ready | 地图、工具栏、状态、HUD |
| S-02 | 空世界；固定相机 | 点住宅 → 指向 tile `(10,10)` → 单击 | 建筑数 1、成功反馈、实体存在 |
| S-03 | 空世界 | 平移 → 缩放 → Q/E 旋转 → 建造 | camera 状态改变、实体 tile 正确 |
| S-04 | `(10,10)` 住宅 | 在 `(11,11)` 再次建造 | occupied、建筑数不变 |
| S-05 | 空世界 | 边缘预览或直接发非法命令 | out-of-bounds、世界 checksum 不变 |
| S-06 | `(10,10)` 住宅并等待保存 | reload | 坐标、朝向、数量一致 |
| S-07 | `saveFormatVersion=999` 或非法结构 | reload | 新游戏、可恢复提示、隔离旧记录 |
| S-08 | 空世界；Worker 测试延时 | 对同一 tile 双击 | 最多一个实体、无重叠 |

## 自动化映射（拟定）

| 层级 | 文件 / 用例组 | 覆盖 |
| --- | --- | --- |
| 单元 | `packages/simulation/src/building-placement.test.ts` | S-02、S-04、S-05、S-08 |
| 协议 | `packages/protocol/src/game-protocol.test.ts` | command/result schema、非法参数 |
| 集成 | `packages/simulation/src/world.integration.test.ts` | S-02、S-04、S-05、S-08 |
| 持久化 | `packages/persistence/src/save-store.integration.test.ts` | S-06、S-07 |
| E2E | `apps/game-web/e2e/first-vertical-slice.spec.ts` | S-01～S-08 的用户可见路径 |

## 验收文档确认（开发闸门）

| 项 | 已写入文档？ | 用户已确认？ |
| --- | --- | --- |
| 成功路径：加载、相机、建造、刷新恢复 | 是 | 是 |
| 失败/边界：重叠、越界、双击、损坏存档 | 是 | 是 |
| 数据与状态：Worker 权威、自动保存、恢复不重复 | 是 | 是 |
| 外层测试：单元、集成、Chromium E2E、跨浏览器冒烟 | 是 | 是 |

确认记录：用户于 2026-07-10 回复“确认第一垂直切片验收”，并授权后续由代理自行验收和持续驱动。结论：**可以进入结构设计与 TDD 产码**。

## 验收执行结果

| 项 | 结果 |
| --- | --- |
| TDD 单元 / 集成 | 第一切片交付后经审计加固为 6 个测试文件、26 条用例全部通过；当前全仓回归 116 条全部通过 |
| Chromium ATDD | S-01～S-08 共 9 条第一切片 E2E 全部通过；补充覆盖静止指针旋转刷新、左/中/右键拖拽不误建造 |
| Firefox / WebKit | S-01 启动与 WebGL2 沙盘冒烟各 1 条，全部通过；WebKit 自动化不等同于原生 Safari 手工验收 |
| 静态质量 | TypeScript strict（含 E2E 与工具配置）、ESLint 零警告、Prettier 全部通过 |
| 生产构建 | Vite 构建通过；Worker 独立产物生成成功；E2E 从 `dist` 预览执行 |
| 手工视觉 | 1440×900 实际渲染检查通过；地面、网格、住宅、预览和 HUD 均可读 |

最终命令：`pnpm test:all`，2026-07-10 在第二切片完成后再次执行全绿。审计加固同时补上 Worker 入站校验、存档语义校验、生产 `dist` E2E、SwiftShader CI 参数与 GitHub Actions。非阻塞观察：Babylon 主入口当前约 1.29 MB minified / 330 KB gzip，Vite 给出大 chunk 提示；后续素材和功能扩展前列入按需加载性能任务。
