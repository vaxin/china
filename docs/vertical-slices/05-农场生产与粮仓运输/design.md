# 第五垂直切片：结构设计

## 数据与版本

- 当前存档版本提升到 v4；冻结 v1/v2/v3 类型与 schema。
- `BuildingView` 增加 `farm(2×2, foodStock 0..3)` 与 `granary(2×2, foodStock 0..10)`。
- 库存属于建筑权威状态；道路距离和运输候选是 tick 内派生数据，不落盘。
- v1/v2/v3 迁移保留旧状态，新增建筑数组为空，因此不凭空产生粮食。

## Tick 相位

1. 校验 tick/revision 仍有安全整数容量并令 tick +1。
2. 当 `tick % 3 === 0` 时，所有未满农场按 ID 顺序各产 1。
3. 按粮仓 ID 顺序处理运输；每仓最多取 1，来源按最短道路距离、农场 ID 选择。
4. 运行既有道路入住与水井升级协调。
5. 任一库存、住户或住宅等级变化时，本 tick 的 revision 只增加 1。

临近安全整数上限时，批量 tick 先在克隆世界按真实变化预演，全部可提交才原子替换；正常计数区间走无克隆快路径。

## 道路运输

- 建筑从四边相邻道路格接入网络。
- 对每个有粮农场生成最短道路距离场，传播上限 12；邻格共同道路的距离为 0。
- 同一粮食只在实际选中来源时从农场扣除并给粮仓增加，单线程权威 tick 不产生重复领取。
- 32×32 原型优先清晰的全量推导；扩大地图后再按道路 revision 缓存或引入运输任务/库存预留。

## 不变量

1. 四类建筑 ID 全局唯一，占地不重叠、不压道路和城门。
2. 农场、粮仓库存必须是范围内整数，删除建筑即删除本地库存。
3. 道路坐标先判地图边界再转 tile key，不能跨边缘环绕。
4. 快照按建筑 ID、道路 key、住户 houseId 稳定排序。
5. 表现层只展示生产与库存，不参与权威计算。
6. 农场到多粮仓时先按粮仓 ID 处理；同一粮仓的来源按道路距离、农场 ID 选择，同一库存单位只会扣减一次。

## TDD 顺序

1. v4 类型、冻结 v3、迁移与库存语义校验。
2. 农场/粮仓占地、ID、拒绝原子性和拆除。
3. 生产节拍、容量、批量 tick 等价。
4. 道路距离 12/13、运输、确定性来源和断路自愈。
5. Worker、Dexie、React HUD、Babylon 表现与 Playwright 闭环。
