# 第七垂直切片：住宅施工与流民营造结构设计

## 1. 边界

权威模拟负责施工阶段、流民位置、道路寻路、入住时机和存档；渲染器只根据快照插值人物位置、播放姿势并创建对应阶段 mesh。不得由 React 计时器或 Babylon 动画自行决定“已建成”。

## 2. 协议与迁移

- `HouseBuildingView` 增加 `constructionStage: 0 | 1 | 2 | 3 | 4`，分别表示宅基地、地基、木架、封顶、成屋。
- `WorldSnapshot` 增加 `migrants: MigrantView[]`。
- `MigrantView` 使用 `houseId` 作为稳定绑定 ID，包含道路格 `x/y` 和 `state: walking | building`；每住宅最多一名。
- 存档版本升为 v6；v5→v6 将所有旧住宅标记为 4，并补空 `migrants`。v1～v4 继续沿既有链路迁到 v5 后再迁 v6。
- v6 强不变量：未完工住宅不可有住户；成屋不可有流民；流民必须引用未完工住宅、位置在地图内且绑定唯一；二级住宅必须为成屋。

## 3. 模拟状态机

每 tick 在既有粮食生产/运输之后、住户消费与住宅升级协调之前执行施工协调：

1. `plot` 且城门道路可达：若无流民，在城门生成 `walking` 流民。
2. `walking`：用道路 BFS 求当前位置到住宅任一相邻道路格的确定性最短路；每 tick 前进一步；到达后切 `building` 并进入地基。
3. `building`：每 tick 阶段 +1，直到成屋；完成时删除流民。
4. 只对成屋执行既有住户迁入、供水升级和供粮协调。
5. 道路拆除时清理位置/路径失效的步行流民；施工流民不受断路影响。住宅拆除时同步清理绑定流民和住户。

状态变化与 tick 内其他变化共同只增加一次 revision；批量 tick 与逐 tick 结果一致。

## 4. 渲染结构

- 将住宅创建拆为 `plot/foundation/frame/roof/complete` 五个可复用 mesh 组合；轮廓、材质和结构来自已批准施工设定稿。
- 完整住宅使用暖灰抹墙、深木梁柱、青灰双坡瓦、克制朱红门；二级住宅在同一基础上增加院墙细节，不用整体放大伪造升级。
- 流民是一个带层级关节的轻量程序化角色：脚底根节点、躯干、头、发髻、四肢、包袱和木槌。`walking` 根据相邻快照插值并摆动四肢；`building` 原地播放锤击。
- `syncWorld` 的签名包含住宅施工阶段；阶段变化先完整 dispose 旧部件再创建新部件。人物 map 以 `houseId` 管理，快照移除即清理。
- 继续使用同一 45°正交相机；新材质的高光接近零，以粗糙、手绘微缩质感为主。

## 5. UI 与自动化

- HUD 住宅计数仍统计全部宅基地；人口只来自成屋住户。
- 增加只读“营造”状态：无施工显示 `营造 —`，否则显示 `营造 n 处`，便于用户和 E2E 观察权威状态。
- Babylon mesh 名称暴露稳定语义：`house-<id>-plot|foundation|frame|roof|complete`、`migrant-<houseId>-root`，E2E 主要断言 HUD/快照和画布像素，不断言内部调用。
- E2E 使用短道路控制总时长，分别截取宅基地、流民、木架和成屋；刷新恢复断言以 IndexedDB v6 快照为准。
