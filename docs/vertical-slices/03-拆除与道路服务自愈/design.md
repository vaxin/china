# 第三垂直切片：结构设计

## 协议

新增 `demolish { seq, type, x, y }`。坐标结构层只要求安全整数，地图越界与空地由 simulation 返回领域拒绝原因；存档世界结构不变，因此继续使用 v2，不伪造新版本。

## 模拟

1. 校验目标在地图内。
2. 优先按 tile key 查找道路；命中则删除单格。
3. 否则查找 footprint 包含目标的住宅；命中则删除住宅及其住户。
4. 都未命中则拒绝，世界深比较不变。
5. 道路拆除后立即剔除失去服务的住户，保证快照可保存；每个 tick 再为已连通但尚未入住的住宅补齐住户。

## 表现与 UI

- renderer 的工具联合扩为 `house | road | demolish | null`；拆除预览固定 1×1，使用琥珀色，空地显示红色。
- App 只调用 `evaluateDemolition` 做预览；真正删除仍由 Worker 判定。
- 拆除、迁出、补路迁入均复用已有 revision 保存队列；空转 tick 不写盘。

## TDD 顺序

1. 协议合法/非法拆除命令。
2. 道路、住宅、空地、越界纯规则。
3. 断路迁出与补路迁入的逐 tick 确定性。
4. Worker 消息顺序。
5. UI/3D 预览、用户反馈与刷新恢复 E2E。
